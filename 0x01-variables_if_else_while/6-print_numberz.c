#include <stdio.h>

/**
 * main - prints tha alphabet in lowercase
 *
 * Return: 0
*/
	int main(void)

	{
		char i;

		for (i = 'a'; i <= 'z'; i++)
		{
			putchar((num % 10) + '0');
		}
		putchar('\n');
		return (0);
}
